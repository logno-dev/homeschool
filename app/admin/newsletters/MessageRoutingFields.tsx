'use client'

export default function MessageRoutingFields({ aliases, senderAlias, replyToAlias, disabled, onSenderChange, onReplyToChange }: { aliases: string[]; senderAlias: string; replyToAlias: string; disabled?: boolean; onSenderChange: (value: string) => void; onReplyToChange: (value: string) => void }) {
  return <div className="grid gap-4 rounded-md border border-gray-200 bg-gray-50 p-4 sm:grid-cols-2"><label className="text-sm font-medium text-gray-700">From<select value={senderAlias} disabled={disabled} onChange={(event) => onSenderChange(event.target.value)} className="mt-1 block w-full rounded-md border border-gray-300 bg-white px-3 py-2 font-normal"><option value="">Use configured default</option>{aliases.map((alias) => <option key={alias} value={alias}>{alias}</option>)}</select></label><label className="text-sm font-medium text-gray-700">Reply-to<select value={replyToAlias} disabled={disabled} onChange={(event) => onReplyToChange(event.target.value)} className="mt-1 block w-full rounded-md border border-gray-300 bg-white px-3 py-2 font-normal"><option value="">No reply-to address</option>{aliases.map((alias) => <option key={alias} value={alias}>{alias}</option>)}</select></label></div>
}
